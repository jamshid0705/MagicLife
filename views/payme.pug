doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
  body 
     form(method='POST' action='https://test.paycom.uz')
        // Идентификатор WEB Кассы
        input(type='hidden' name='merchant' value=`${ merchant_id}`)
        // Сумма платежа в тийинах
        input(type='hidden' name='amount' value=`${data.amount*100}`)
        // Поля Объекта Account
        input(type='hidden' name='account[id]' value=`${data.account}`)
        // ==================== НЕОБЯЗАТЕЛЬНЫЕ ПОЛЯ ======================
        //
          Язык. Доступные значения: ru|uz|en
          Другие значения игнорируются
          Значение по умолчанию ru
        input(type='hidden' name='lang' value='ru')
    
        // ==================================================================
        button(type='submit')
          | &Ocy;&pcy;&lcy;&acy;&tcy;&icy;&tcy;&softcy; &scy; &pcy;&ocy;&mcy;&ocy;&shchcy;&softcy;&yucy; 
          b Payme
